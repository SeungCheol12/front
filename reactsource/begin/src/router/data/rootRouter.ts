import { createBrowserRouter } from 'react-router-dom';

// http://localhost5173/products/~~
const rootRouter = createBrowserRouter([
  {
    // path: '/products',
    // Component: Products,
    // children: productRouter(),
  },
]);

export default rootRouter;
